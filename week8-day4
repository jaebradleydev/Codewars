Input -> Output
"8 j 8   mBliB8g  imjB8B8  jl  B" -> "8j8mBliB8gimjB8B8jlB"
"8 8 Bi fk8h B 8 BB8B B B  B888 c hl8 BhB fd" -> "88Bifk8hB8BB8BBBB888chl8BhBfd"
"8aaaaa dddd r     " -> "8aaaaaddddr"

function noSpace(x){
  //We can split the string into an array, adding " " as a specification, which wiill treat the spaces already present in the string as dividers that will not be elements within the array
   return x.split(" ").join("")
  //Then the join method with "" will put everything together without any dividers, including any spaces
}
