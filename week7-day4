DESCRIPTION:
Make your strings more nerdy: Replace all 'a'/'A' with 4, 'e'/'E' with 3 and 'l' with 1 e.g. "Fundamentals" --> "Fund4m3nt41s"


  function nerdify(txt){
  //We can use regex rules within the replace string method to apply the changes to any given character with g (and i in the instances where we ignore case sensitivity)
  return txt.replace(/a/gi, "4").replace(/e/gi, "3").replace(/l/g, "1")
}
